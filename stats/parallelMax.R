# pmax() and pmin()
# Returns the parallel maxima and minima of the input values
pmin(c(1,0,-1), -2)
pmax(c(1,0,-1), -2)
pmax(1,2)
max(1,2)
pmax(c(1,5), 4)
pmax(c(-1,1), 0) # returns a vector c(0, 1)
